# Bibliotheca Illustration Studio

This repository contains the public interface for the local illustration studio.
The interface does not contain API keys or server-side generation code. When it
is opened from GitHub Pages, it connects to the local `studio_server.py` running
on `127.0.0.1:5000`.
